// Base URL các API. Khai báo giá trị trong file `.env` (xem `.env.example`).
// Thêm backend mới thì thêm 1 dòng ở đây + 1 repository tương ứng trong
// `src/_base/_const/_constVar.tsx`.
export const BASE_API_URL = process.env.REACT_APP_API_URL;
export const BASE_API_AUTH_URL = process.env.REACT_APP_AUTH_API_URL;
